import { loadOptions } from "./loadOptions.js";
import { createClerkClient } from "@clerk/backend";

//#region src/server/clerkClient.ts
const clerkClient = (args, overrides = {}) => {
	const { apiUrl, secretKey, jwtKey, proxyUrl, isSatellite, domain, publishableKey, machineSecretKey } = loadOptions(args, overrides);
	return createClerkClient({
		apiUrl,
		secretKey,
		jwtKey,
		proxyUrl,
		isSatellite,
		domain,
		publishableKey,
		machineSecretKey,
		userAgent: `@clerk/react-router@3.4.5`
	});
};

//#endregion
export { clerkClient };
//# sourceMappingURL=clerkClient.js.map