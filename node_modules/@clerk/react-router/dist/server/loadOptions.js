import { noSecretKeyError, satelliteAndMissingProxyUrlAndDomain, satelliteAndMissingSignInUrl } from "../utils/errors.js";
import { getPublicEnvVariables } from "../utils/env.js";
import { canUseKeyless } from "../utils/feature-flags.js";
import { patchRequest } from "./utils.js";
import { getEnvVariable } from "@clerk/shared/getEnvVariable";
import { createClerkRequest } from "@clerk/backend/internal";
import { apiUrlFromPublishableKey } from "@clerk/shared/apiUrlFromPublishableKey";
import { isDevelopmentFromSecretKey } from "@clerk/shared/keys";
import { isHttpOrHttps, isProxyUrlRelative } from "@clerk/shared/proxy";
import { handleValueOrFn } from "@clerk/shared/utils";

//#region src/server/loadOptions.ts
const loadOptions = (args, overrides = {}) => {
	const { request, context } = args;
	const clerkRequest = createClerkRequest(patchRequest(request));
	const secretKey = overrides.secretKey || getEnvVariable("CLERK_SECRET_KEY", context);
	const machineSecretKey = overrides.machineSecretKey || getEnvVariable("CLERK_MACHINE_SECRET_KEY", context);
	const publishableKey = overrides.publishableKey || getPublicEnvVariables(context).publishableKey;
	const jwtKey = overrides.jwtKey || getEnvVariable("CLERK_JWT_KEY", context);
	const apiUrl = getEnvVariable("CLERK_API_URL", context) || apiUrlFromPublishableKey(publishableKey);
	const domain = handleValueOrFn(overrides.domain, new URL(request.url)) || getPublicEnvVariables(context).domain;
	const isSatellite = handleValueOrFn(overrides.isSatellite, new URL(request.url)) || getPublicEnvVariables(context).isSatellite;
	const relativeOrAbsoluteProxyUrl = handleValueOrFn(overrides?.proxyUrl, clerkRequest.clerkUrl, getPublicEnvVariables(context).proxyUrl);
	const signInUrl = overrides.signInUrl || getPublicEnvVariables(context).signInUrl;
	const signUpUrl = overrides.signUpUrl || getPublicEnvVariables(context).signUpUrl;
	const signInForceRedirectUrl = overrides.signInForceRedirectUrl || getPublicEnvVariables(context).signInForceRedirectUrl;
	const signUpForceRedirectUrl = overrides.signUpForceRedirectUrl || getPublicEnvVariables(context).signUpForceRedirectUrl;
	const signInFallbackRedirectUrl = overrides.signInFallbackRedirectUrl || getPublicEnvVariables(context).signInFallbackRedirectUrl;
	const signUpFallbackRedirectUrl = overrides.signUpFallbackRedirectUrl || getPublicEnvVariables(context).signUpFallbackRedirectUrl;
	let proxyUrl;
	if (!!relativeOrAbsoluteProxyUrl && isProxyUrlRelative(relativeOrAbsoluteProxyUrl)) proxyUrl = new URL(relativeOrAbsoluteProxyUrl, clerkRequest.clerkUrl).toString();
	else proxyUrl = relativeOrAbsoluteProxyUrl;
	if (!secretKey && !canUseKeyless) throw new Error(noSecretKeyError);
	if (isSatellite && !proxyUrl && !domain) throw new Error(satelliteAndMissingProxyUrlAndDomain);
	if (isSatellite && secretKey && !isHttpOrHttps(signInUrl) && isDevelopmentFromSecretKey(secretKey)) throw new Error(satelliteAndMissingSignInUrl);
	return {
		...overrides,
		secretKey,
		machineSecretKey,
		publishableKey,
		jwtKey,
		apiUrl,
		domain,
		isSatellite,
		proxyUrl,
		signInUrl,
		signUpUrl,
		signInForceRedirectUrl,
		signUpForceRedirectUrl,
		signInFallbackRedirectUrl,
		signUpFallbackRedirectUrl
	};
};

//#endregion
export { loadOptions };
//# sourceMappingURL=loadOptions.js.map