import { invalidClerkStatePropError, noClerkStateError, publishableKeyMissingErrorInSpaMode } from "./errors.js";

//#region src/utils/assert.ts
function warnForSsr(val) {
	if (!val || !val.__internal_clerk_state) console.warn(noClerkStateError);
}
function assertEnvVar(name, errorMessage) {
	if (!name || typeof name !== "string") throw new Error(errorMessage);
}
function assertValidClerkState(val) {
	if (!val) throw new Error(noClerkStateError);
	if (!!val && !val.__internal_clerk_state) throw new Error(invalidClerkStatePropError);
}
function assertPublishableKeyInSpaMode(key) {
	if (!key || typeof key !== "string") throw new Error(publishableKeyMissingErrorInSpaMode);
}
/**
* When React Router is used as a framework, the __reactRouterContext object is populated on window.
* The isSpaMode will then be set to true if you use https://reactrouter.com/how-to/spa or when you deactivate SSR
*
* When React Router is used as a library the __reactRouterContext object is not populated on window at all.
*
* So in library mode we can't determine if we are in SPA mode or not so we return undefined.
*/
const isSpaMode = () => {
	if (typeof window !== "undefined" && typeof window.__reactRouterContext?.isSpaMode !== "undefined") return window.__reactRouterContext.isSpaMode;
};

//#endregion
export { assertEnvVar, assertPublishableKeyInSpaMode, assertValidClerkState, isSpaMode, warnForSsr };
//# sourceMappingURL=assert.js.map