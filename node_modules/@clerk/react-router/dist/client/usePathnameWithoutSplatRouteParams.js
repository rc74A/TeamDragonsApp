import { useRef } from "react";
import { useLocation, useParams } from "react-router";

//#region src/client/usePathnameWithoutSplatRouteParams.tsx
const usePathnameWithoutSplatRouteParams = () => {
	const params = useParams();
	const { pathname } = useLocation();
	const splatRouteParam = params["*"] || "";
	return useRef(`/${pathname.replace(splatRouteParam, "").replace(/\/$/, "").replace(/^\//, "").trim()}`).current;
};

//#endregion
export { usePathnameWithoutSplatRouteParams };
//# sourceMappingURL=usePathnameWithoutSplatRouteParams.js.map